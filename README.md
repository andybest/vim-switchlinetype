A plugin to toggle the line numbering type in vim

To use, run the following:
```
:call ToggleLineNumbers()
```

I map it to a leader key as follows:
```
map <leader>r :call ToggleLineNumbers()<cr>
```
